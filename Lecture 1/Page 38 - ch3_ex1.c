#include <stdio.h>

int main() {
    // Declare variables
    int i, j;
    float x, y;

    // Assign values to variables
    i = 10;
    j = 20;
    x = 43.2892f;
    y = 5527.0f;

    // Print the values of variables
    printf("i = %d, j = %d, x = %f, y = %f\n", i, j, x, y);

    return 0;
}
